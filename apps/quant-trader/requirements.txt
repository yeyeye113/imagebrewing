pandas>=2.0
numpy>=1.24
PyYAML>=6.0
# Optional data source (Yahoo Finance). The app falls back to synthetic data if missing/offline.
yfinance>=0.2.40
# Optional real broker API (paper + live trading). Only needed for `live` command.
alpaca-py>=0.20
# API server (lets external AI agents connect) + AI-strategy HTTP client.
fastapi>=0.110
uvicorn>=0.29
websockets>=12.0
requests>=2.31
# Optional A-share (China) market data, free, no token. Needed for data_source: akshare.
akshare>=1.12
# ML strategy (optional — only needed for strategy: ml)
scikit-learn>=1.4
xgboost>=2.0
joblib>=1.3

# 期权定价
scipy>=1.11

# 性能监控
psutil>=5.9

# 数据可视化
matplotlib>=3.7

# 代码质量
ruff>=0.1.0
black>=23.0
isort>=5.12

# 类型检查
mypy>=1.5

# 测试
pytest>=7.4
pytest-cov>=4.1
pytest-benchmark>=4.0

# 文档
sphinx>=7.0
